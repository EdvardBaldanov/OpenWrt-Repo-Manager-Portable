# OpenWrt Repo Manager (Portable)

Автоматизированный менеджер локального репозитория для OpenWrt. Скрипт сам скачивает последние релизы указанных пакетов с GitHub, индексирует их, подписывает ключом `usign` и предоставляет удобную веб-страницу для настройки роутера.

## ✨ Особенности

* **Portable**: Все необходимые бинарники (`usign`, `opkg-utils`) лежат в папке `bin/`. Не нужно ничего устанавливать в систему.
* **Умное обновление**: Скачивает файлы в `/tmp` и обновляет репозиторий на диске только при появлении новой версии.
* **Прозрачность**: Детальные логи работы доступны прямо через веб-интерфейс.
* **Автоматизация**: Полная настройка Nginx и планировщика Cron одной командой.
* **JSON Index**: Генерация `index.json` для каждой архитектуры для интеграции с внешними сервисами.

## 📁 Структура проекта

* `bin/` — портативные утилиты (`usign`, `opkg-make-index` и др.).
* `repo_sources.json` — конфигурация источников (какие репозитории GitHub мониторить).
* `setup_repo.sh` — скрипт первичной настройки сервера.
* `repo_update.sh` — основной скрипт обновления пакетов.
* `index.html` — шаблон веб-интерфейса.

## 🚀 Быстрый старт

### 1. Подготовка

Склонируйте репозиторий и убедитесь, что в папке `bin/` находятся необходимые бинарники.
```bash
apt install -y git sudo
```
```bash
git clone https://github.com/EdvardBaldanov/OpenWrt-Repo-Manager-Portable.git
```
### 2. Установка

Запустите скрипт инициализации:

```bash
chmod +x setup_repo.sh repo_update.sh
```
```bash\
./setup_repo.sh

```

Скрипт настроит Nginx, создаст ключи подписи и добавит задачу в Cron.

### 3. Первое обновление

Запустите обновление вручную, чтобы наполнить репозиторий:

```bash
./repo_update.sh

```

## 🌐 Использование

Откройте в браузере IP-адрес вашего сервера (например, `http://192.168.1.10/`). Там вы найдете:

1. Готовые команды для копирования в терминал роутера.
2. Лог последних обновлений в реальном времени.

## ⚙️ Добавление новых пакетов

Чтобы добавить новый пакет, просто внесите его в `repo_sources.json`.
Пример блока:

```json
{
  "name": "my_package",
  "api_url": "https://api.github.com/repos/user/repo/releases/latest",
  "filter_arch": "x86_64",
  "exclude_asset_keywords": ["entware", "arm"]
}

```

## 🛡 Безопасность

Публичный ключ `public.key` генерируется автоматически и доступен для скачивания роутерами. Приватный ключ `secret.key` никогда не должен покидать сервер и добавлен в `.gitignore`.
